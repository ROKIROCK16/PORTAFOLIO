# Habilita la escritura del panel /studio en produccion.
# Solo tiene sentido en hosts con filesystem persistente (VPS, Docker, Render Disk).
# En serverless (Vercel/Netlify) el FS es de solo lectura y Studio respondera 503.
ENABLE_STUDIO=false
